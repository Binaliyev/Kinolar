const elementSelect = (element) =>{ 
    return( 
        document.querySelector(element)
    )
}
const elementSelectAll = (element) =>{ 
    return( 
        document.querySelectorAll(element)
    )
}
const creatTag = (tag) => {
    return(
        document.createElement(tag)
    )
}
